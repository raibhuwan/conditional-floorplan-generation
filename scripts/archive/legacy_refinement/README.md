# Legacy Refinement Scripts

These scripts are retained as development history only and are not used for the final dissertation experiments.

The final refinement implementations are:

- `src/refinement/morphology.py`
- `src/refinement/hillclimb.py`

The final evaluation routes are:

- `scripts/evaluate_unet_morphology.py`
- `scripts/evaluate_cgan_morphology.py`
- `scripts/evaluate_unet_hillclimb.py`
- `scripts/evaluate_cgan_hillclimb.py`

The legacy scripts contain earlier experimental settings and should not be used to reproduce the final dissertation results.
